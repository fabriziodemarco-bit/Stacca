<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Permesso per allarmi esatti: SCHEDULE_EXACT_ALARM viene gestito runtime (flusso in MainActivity)
         USE_EXACT_ALARM rimosso — rischio rigetto Play Store policy -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- SYSTEM_ALERT_WINDOW rimosso — non usato, abbiamo già USE_FULL_SCREEN_INTENT -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:name=".StaccaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stacca"
        tools:targetApi="35">

        <!-- Splash Screen (LAUNCHER) -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Stacca">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.Stacca">

            <!-- Deep link per conferma email Supabase -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="stacca"
                    android:host="login-callback" />
            </intent-filter>
        </activity>
          
               <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Stacca" />



        <!-- Full Screen Alert Activity -->
        <activity
            android:name=".ui.FullScreenAlertActivity"
            android:exported="false"
            android:theme="@style/Theme.Stacca.FullScreen"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance" />



        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Stacca"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Premium Activity -->
        <activity
            android:name=".ui.PremiumActivity"
            android:exported="false"
            android:theme="@style/Theme.Stacca"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Paywall Activity -->
        <activity
            android:name=".ui.PaywallActivity"
            android:exported="false"
            android:theme="@style/Theme.Stacca"
            android:launchMode="singleTop" />

        <!-- Trial Expired Activity — blocca accesso dopo 5 giorni senza account -->
        <activity
            android:name=".ui.TrialExpiredActivity"
            android:exported="false"
            android:theme="@style/Theme.Stacca"
            android:launchMode="singleTop" />



        <!-- Alarm Receiver -->
        <receiver
            android:name=".receivers.AlarmReceiver"
            android:exported="false" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Notification Action Receiver -->
        <receiver
            android:name=".receivers.NotificationActionReceiver"
            android:exported="false" />

    </application>

</manifest>
